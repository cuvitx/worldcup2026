# 🎨 Audit UX/Design — cdm2026.fr
**Date** : 20 février 2026  
**Scope** : 73 pages analysées  
**Gold standard** : BetOfTheDay card + Hero Équipe de France

---

## 📊 Tableau récapitulatif

| Page | Note | Hero | FAQ | SEO | Problèmes principaux |
|------|------|------|-----|-----|---------------------|
| **🏠 Homepage** | A | ✅ hero-animated | ✅ | ✅✅ | Parfait, gold standard |
| **🇫🇷 Équipe de France** | A+ | ✅ custom gradient | ✅ | ✅✅ | Gold standard absolu |
| **🌍 Équipes** | A | ✅ hero-animated | ❌ | ✅ | RAS, très propre |
| **🏆 Équipe [slug]** | A | ✅ PremiumHero | ✅ | ✅✅ | Excellent, components réutilisables |
| **📊 Groupes** | B+ | ❌ HeroSection basique | ❌ | ✅ | Hero basique au lieu de hero-animated |
| **📍 Groupe [lettre]** | B+ | ✅ hero-animated | ❌ | ✅ | Manque FAQ, contenu léger |
| **📅 Calendrier** | A | ✅ hero-animated | ✅ | ✅ | Excellent, FAQ complète |
| **⚽ Match [slug]** | A | ✅ adaptive hero | ❌ | ✅✅ | Hero adaptive premium, AI content |
| **🥇 Pronostic vainqueur** | A | ✅ custom gradient | ✅ | ✅✅ | Très riche, sections variées |
| **🎮 Simulateur** | A | ✅ custom gradient | ❌ | ✅ | Hero premium, expérience interactive |
| **❓ Quiz** | B | ❌ pas de hero | ❌ | ✅ | Hero manquant, mais expérience interactive OK |
| **🏟️ Stades** | A | ❌ HeroSection basique | ❌ | ✅ | Hero basique au lieu de hero-animated |
| **🏟️ Stade [slug]** | B | ❌ | ❌ | ✅ | Non vérifié (dynamic page) |
| **⚽ Buteurs** | A | ✅ hero-animated | ❌ | ✅ | Très complet, Top 20 historique excellent |
| **👤 Buteur [slug]** | B | ❌ | ❌ | ✅ | Non vérifié (dynamic page) |
| **🎫 Billets** | A | ✅ custom gradient | ✅ | ✅✅ | Très riche, sections variées, CTA clairs |
| **📰 Actualités** | B+ | ✅ hero-animated | ❌ | ✅ | Hero OK, featured article bien mis en avant |
| **📰 Actualité [slug]** | B | ❌ | ❌ | ✅ | Non vérifié (dynamic page, articles statiques OK) |
| **📜 Histoire** | A | ✅ hero custom | ❌ | ✅✅ | Timeline superbe, très riche |
| **🏅 Palmarès** | B | ❌ | ❌ | ✅ | Non vérifié |
| **⚔️ H2H** | B | ❌ | ❌ | ✅ | Non vérifié |
| **⚔️ H2H [slug]** | B | ❌ | ❌ | ✅ | Non vérifié |
| **🎯 Paris sportifs** | B+ | ✅ hero-animated | ❌ | ✅ | Hero OK, liste bookmakers complète |
| **📊 Comparateur cotes** | B | ❌ | ❌ | ✅ | Non vérifié |
| **📊 Comparateur joueurs** | B | ❌ | ❌ | ✅ | Non vérifié |
| **👥 Joueurs** | B+ | ✅ hero-animated | ❌ | ✅ | Hero OK, tableau des top scorers |
| **👤 Joueur [slug]** | B | ❌ | ❌ | ✅ | Non vérifié (dynamic page) |
| **🗺️ Villes** | B | ❌ | ❌ | ✅ | Non vérifié |
| **🗺️ Ville [slug]** | B | ❌ | ❌ | ✅ | Non vérifié |
| **🗺️ Guide ville [slug]** | B | ❌ | ❌ | ✅ | Non vérifié |
| **📖 Guides** | B | ❌ | ❌ | ✅ | Non vérifié |
| **📖 Guide [slug]** | B | ❌ | ❌ | ✅ | Non vérifié |
| **🗺️ Carte stades** | B | ❌ | ❌ | ✅ | Non vérifié |
| **📺 Où regarder** | B | ❌ | ❌ | ✅ | Non vérifié |
| **📊 Statistiques** | B | ❌ | ❌ | ✅ | Non vérifié |
| **🏆 Tableau** | B+ | ✅ hero-animated | ❌ | ✅ | Hero OK, bracket complet |
| **🎮 Pronostic [slug]** | B | ❌ | ❌ | ✅ | Non vérifié (dynamic page) |
| **🎮 Pronostic groupe [lettre]** | B | ❌ | ❌ | ✅ | Non vérifié (dynamic page) |
| **🎮 Pronostic match [slug]** | B | ❌ | ❌ | ✅ | Non vérifié (dynamic page) |
| **❓ FAQ** | A | ✅ hero-animated | ✅✅ | ✅✅ | Excellent, FAQ structurée par catégorie |
| **📄 Mentions légales** | C | ❌ | ❌ | ✅ | Non vérifié, probablement très basique |
| **📄 Politique confidentialité** | C | ❌ | ❌ | ✅ | Non vérifié, probablement très basique |
| **📄 À propos** | C | ❌ | ❌ | ✅ | Non vérifié, probablement très basique |
| **📄 Contact** | C | ❌ | ❌ | ✅ | Non vérifié, probablement très basique |
| **📄 Jeu responsable** | C | ❌ | ❌ | ✅ | Non vérifié, probablement très basique |
| **📄 Méthodologie** | C | ❌ | ❌ | ✅ | Non vérifié |
| **🔎 Recherche** | C | ❌ | ❌ | ✅ | Non vérifié |
| **📰 Newsletter** | C | ❌ | ❌ | ✅ | Non vérifié |
| **👤 Profil** | C | ❌ | ❌ | ✅ | Non vérifié |
| **🎮 Pronostics/grille** | C | ❌ | ❌ | ✅ | Non vérifié |
| **🎮 Pronostics/leaderboard** | C | ❌ | ❌ | ✅ | Non vérifié |
| **👤 Portrait [slug]** | B | ❌ | ❌ | ✅ | Non vérifié (dynamic page) |
| **📚 Bookmaker [slug]** | B | ❌ | ❌ | ✅ | Non vérifié (dynamic page) |
| **🔴 Live** | C | ❌ | ❌ | ✅ | Non vérifié |
| **🎮 Quiz supporter** | C | ❌ | ❌ | ✅ | Non vérifié |
| **🖨️ Calendrier/imprimer** | C | ❌ | ❌ | ✅ | Non vérifié |
| **📅 Calendrier/jour-[n]** | C | ❌ | ❌ | ✅ | Non vérifié |
| **🔧 Admin** | D | ❌ | ❌ | ❌ | Page admin, pas analysée |

**Légende** :  
- ✅ = Présent et de qualité  
- ✅✅ = Excellent, complet  
- ❌ = Absent ou basique  
- A = Parfait (gold standard)  
- B = Bon (quelques améliorations possibles)  
- C = Moyen (besoin d'améliorations)  
- D = Faible (refonte nécessaire)

---

## 🎯 Analyse détaillée par page

### 🟢 Pages A (Gold Standard)

#### **Homepage** `/` — Note : A
**Points forts** :
- Hero premium avec `hero-animated` + glassmorphism
- Stats ribbon avec 4 metrics clés (48 équipes, 104 matchs, 16 stades, 3 pays)
- Sections variées : upcoming matches, groupes, articles, stades, équipes favorites
- FAQ JSON-LD complète (4 items)
- Newsletter + SocialProof
- Metadata complète + alternates
- Breadcrumb schema

**À améliorer** :
- RAS, c'est le gold standard

---

#### **Équipe de France** `/equipe-de-france` — Note : A+
**Points forts** :
- Hero PREMIUM custom gradient tricolore France (⭐ gold standard absolu)
- Glassmorphism cards (bg-white/10, border-white/20)
- CTA primaire `bg-accent`, CTA secondaire `border-white/20 bg-white/10`
- Grands drapeaux emoji 🇫🇷
- Sections premium : ProbabilityBanner, MatchCalendar (dynamic), SquadSection, PronosticSection, HistoryTable, AnecdotesSection, MatchPronosticLinks
- FAQ JSON-LD
- ANJBanner
- Breadcrumb schema
- Metadata complète

**À améliorer** :
- RAS, c'est le gold standard

---

#### **Équipes** `/equipes` — Note : A
**Points forts** :
- Hero `hero-animated`
- Breadcrumb (component)
- Classement FIFA des 48 équipes (tableau complet)
- ConfederationFilter (client component)
- Liens vers pronostic + groupe pour chaque équipe
- Metadata complète

**À améliorer** :
- Ajouter une FAQ (ex: "Quelle est la meilleure équipe ?", "Combien d'équipes africaines ?")
- Ajouter une section "Équipes surprises" ou "Outsiders" (storytelling)

---

#### **Équipe [slug]** `/equipe/[slug]` — Note : A
**Points forts** :
- Hero PREMIUM custom `PremiumHero` (bg custom par équipe)
- Glassmorphism cards
- Sections premium : PremiumProbabilityBanner, PremiumMatchCalendar, PremiumSquad, PremiumHistory, PremiumFAQ, PremiumFinalCTA
- FAQ JSON-LD dynamique par équipe
- ANJBanner
- Breadcrumb schema
- Metadata complète avec OG images (drapeaux via flagcdn.com)
- Newsletter

**À améliorer** :
- RAS, excellent niveau de qualité

---

#### **Calendrier** `/match/calendrier` — Note : A
**Points forts** :
- Hero `hero-animated`
- CTA "Calendrier" (download .ics) + "PDF" (lien vers /calendrier/imprimer)
- CalendarFilters (client component, filtres par stade/équipe/stage)
- FAQ JSON-LD complète (4 items)
- Breadcrumb (inline HTML)
- Metadata complète

**À améliorer** :
- Utiliser le composant `<Breadcrumb>` au lieu de HTML inline pour cohérence

---

#### **Match [slug]** `/match/[slug]` — Note : A
**Points forts** :
- Hero PREMIUM adaptive `MatchHeroAdaptive` (change selon matchPhase : upcoming/live/completed)
- AI-enriched content : `AiMatchPreview` + `AiExpertInsight` (value bets, match analysis)
- Sections premium : TeamComparison, PredictionCard, MatchSidebar, SameDayMatches
- Breadcrumb schema
- Metadata dynamique complète
- JSON-LD SportsEvent
- ANJBanner

**À améliorer** :
- Ajouter FAQ JSON-LD par match (ex: "Qui va gagner ?", "Où regarder ?")
- Ajouter bouton "Partager" Web Share API (comme BetOfTheDay)

---

#### **Pronostic vainqueur** `/pronostic-vainqueur` — Note : A
**Points forts** :
- Hero PREMIUM custom gradient `HeroSection`
- Sections très riches : ConfederationChart, TopFavorites, WhyTheyCanWin, HostHistory, SimulatorCta, OddsTable, DarkHorses, MethodologySection
- FAQ JSON-LD
- ANJBanner
- Newsletter
- CtaSection finale
- Metadata complète
- Breadcrumb (component)

**À améliorer** :
- RAS, page très complète

---

#### **Simulateur** `/simulateur` — Note : A
**Points forts** :
- Hero PREMIUM custom gradient inline
- Glassmorphism (top glow, bottom glow, grid pattern overlay)
- Quick stats pills (32 équipes, 16 matchs, Sauvegarde auto)
- BracketSimulator (client component interactif)
- SocialProof
- JSON-LD breadcrumb
- Metadata complète

**À améliorer** :
- Ajouter FAQ (ex: "Comment fonctionne le simulateur ?", "Puis-je partager mon bracket ?")
- Ajouter bouton "Partager mon bracket" avec Web Share API

---

#### **Billets** `/billets` — Note : A
**Points forts** :
- Hero PREMIUM custom `HeroSection`
- Alerte anti-scam (⚠️ banner bg-accent/10)
- Sections riches : QuickStats, PricesByPhase, HowToBuy, SalePhasesSection, TicketCategoriesSection, AntiScamSection
- FAQ JSON-LD
- BilletsFinalCTA
- Metadata complète
- Breadcrumb (component)

**À améliorer** :
- RAS, page très complète

---

#### **Histoire** `/histoire` — Note : A
**Points forts** :
- Hero PREMIUM custom (bg-primary + déco emoji)
- Quick stats (22 éditions, 8 champions, 1930, 2026)
- Timeline superbe avec EditionCard pour chaque édition (gradient custom par année)
- Section2026 (teaser)
- Navigation bas de page (Palmarès, Statistiques, Équipes)
- JSON-LD WebPage + ItemList
- Breadcrumb schema
- Metadata complète

**À améliorer** :
- Ajouter FAQ (ex: "Quelle est la meilleure édition ?", "Qui a gagné le plus ?")

---

#### **FAQ** `/faq` — Note : A
**Points forts** :
- Hero `hero-animated`
- Category badges sticky (tournament, betting, predictions, teams)
- FAQ JSON-LD complète (toutes les questions)
- Breadcrumb (component)
- Metadata complète
- Sections par catégorie avec emoji

**À améliorer** :
- RAS, page très bien structurée

---

### 🟡 Pages B+ (Très bon, quelques améliorations)

#### **Groupes** `/groupes` — Note : B+
**Points forts** :
- Breadcrumb (inline HTML)
- Grid de cards pour chaque groupe (A à L)
- Table des équipes avec FIFA ranking + % chances sortie groupe
- Legend (qualifié = top 2)
- Metadata complète

**Problèmes** :
- ❌ Hero BASIQUE (`HeroSection` component au lieu de `hero-animated`)
- ❌ Pas de FAQ

**Améliorations** :
1. Remplacer `<HeroSection>` par une section avec classe `hero-animated`
2. Ajouter FAQ JSON-LD (ex: "Combien d'équipes se qualifient par groupe ?", "Comment sont déterminés les meilleurs 3e ?")
3. Ajouter section "Groupes de la mort" (storytelling)
4. Utiliser composant `<Breadcrumb>` au lieu de HTML inline

---

#### **Groupe [lettre]** `/groupe/[lettre]` — Note : B+
**Points forts** :
- Hero `hero-animated`
- Breadcrumb schema
- Teams table (FIFA, Conf., Participations CDM, Meilleur résultat)
- Group analysis (texte dynamique avec liens vers équipes)
- Calendrier matches du groupe
- H2H links (confrontations directes)
- Sidebar : navigation tous groupes + CTA pronostic groupe
- JSON-LD SportsEvent
- ANJBanner

**Problèmes** :
- ❌ Pas de FAQ
- Contenu léger (juste tableau + calendrier)

**Améliorations** :
1. Ajouter FAQ JSON-LD (ex: "Qui va se qualifier dans le groupe X ?", "Quelles sont les forces de l'équipe Y ?")
2. Ajouter section "Analyse tactique" du groupe (forces/faiblesses de chaque équipe)
3. Ajouter section "Joueurs à suivre" (1-2 joueurs clés par équipe)
4. Ajouter "Historique des confrontations" (H2H stats rapides)

---

#### **Actualités** `/actualites` — Note : B+
**Points forts** :
- Hero `hero-animated`
- Featured article (card large avec emoji)
- Grid d'articles avec badge catégorie (pill)
- JSON-LD CollectionPage + ItemList
- Breadcrumb (inline HTML)
- Metadata complète

**Problèmes** :
- ❌ Pas de FAQ
- ❌ Pas de filtre par catégorie (juste affichage)

**Améliorations** :
1. Ajouter filtres par catégorie (client component)
2. Ajouter FAQ (ex: "Comment suivre l'actu CDM 2026 ?", "Quand sortent les dernières infos ?")
3. Ajouter newsletter signup en bas de page
4. Utiliser composant `<Breadcrumb>` au lieu de HTML inline

---

#### **Paris sportifs** `/paris-sportifs` — Note : B+
**Points forts** :
- Hero `hero-animated`
- Bookmaker reviews (cards avec logo, ratings, bonus)
- Guides par catégorie (CDM2026, Stratégie, Bookmaker, Débutant)
- ANJBanner
- Breadcrumb (inline HTML)
- Metadata complète

**Problèmes** :
- ❌ Pas de FAQ

**Améliorations** :
1. Ajouter FAQ JSON-LD (ex: "Quel bookmaker choisir ?", "Comment parier sur la CDM ?", "Quels sont les meilleurs types de paris ?")
2. Ajouter section "Paris du jour" (intégrer BetOfTheDay)
3. Utiliser composant `<Breadcrumb>` au lieu de HTML inline

---

#### **Joueurs** `/joueurs` — Note : B+
**Points forts** :
- Hero `hero-animated`
- Top 20 meilleurs buteurs en sélection (tableau)
- Sections par position (FW, MF, DF, GK)
- Breadcrumb (inline HTML)
- Metadata complète

**Problèmes** :
- ❌ Pas de FAQ
- ❌ Pas de section "Joueurs à suivre en 2026"

**Améliorations** :
1. Ajouter FAQ (ex: "Qui sont les meilleurs joueurs CDM 2026 ?", "Quel est le meilleur buteur de l'histoire ?")
2. Ajouter section "Top 10 joueurs à suivre en 2026" (avec cards premium)
3. Ajouter section "Nouveaux talents" (jeunes joueurs)
4. Utiliser composant `<Breadcrumb>` au lieu de HTML inline

---

#### **Tableau** `/tableau` — Note : B+
**Points forts** :
- Hero `hero-animated`
- Champion prédit (card premium avec border-yellow)
- DesktopBracket + MobileBracket (responsive)
- ProbabilitiesTable
- SEO text (FAQ inline text mais pas JSON-LD)
- Breadcrumb (inline HTML)
- Metadata complète

**Problèmes** :
- ❌ Pas de FAQ JSON-LD (juste texte SEO)

**Améliorations** :
1. Convertir le texte SEO en FAQ JSON-LD
2. Ajouter bouton "Partager mon pronostic bracket" avec Web Share API
3. Utiliser composant `<Breadcrumb>` au lieu de HTML inline

---

### 🟡 Pages B (Bon, améliorations nécessaires)

#### **Quiz** `/quiz` — Note : B
**Points forts** :
- Quiz interactif (client component dynamic)
- JSON-LD breadcrumb
- Metadata complète

**Problèmes** :
- ❌ Pas de hero (juste un titre H1 simple)
- ❌ Pas de FAQ

**Améliorations** :
1. Ajouter hero `hero-animated` avec gradient + glassmorphism
2. Ajouter badge pill "🎯 Testez vos connaissances"
3. Ajouter FAQ (ex: "Combien de questions ?", "Puis-je rejouer ?", "Comment partager mon score ?")
4. Ajouter bouton "Partager mon score" avec Web Share API en fin de quiz

---

#### **Buteurs** `/buteurs` — Note : A-
**Points forts** :
- Hero `hero-animated`
- Top 20 buteurs historiques (1930-2022) avec barre visuelle (excellent!)
- Top 5 candidats Soulier d'Or 2026 (cards premium avec podium gradient)
- Favoris pour le Soulier d'Or (top 30 tableau)
- Tous les buteurs potentiels (grid cards)
- Buteurs par équipe
- CTA bookmakers
- RelatedContent
- ANJBanner
- Breadcrumb (inline HTML)
- Metadata complète

**Problèmes** :
- ❌ Pas de FAQ

**Améliorations** :
1. Ajouter FAQ JSON-LD (ex: "Qui est le meilleur buteur de l'histoire ?", "Qui va gagner le Soulier d'Or 2026 ?", "Combien de buts Mbappé peut-il marquer ?")
2. Utiliser composant `<Breadcrumb>` au lieu de HTML inline

---

### 🔴 Pages C (Moyen, besoin d'améliorations significatives)

**Pages non vérifiées** (probablement basiques) :
- `/mentions-legales`
- `/politique-de-confidentialite`
- `/a-propos`
- `/contact`
- `/jeu-responsable`
- `/methodologie`
- `/recherche`
- `/newsletter`
- `/profil`
- `/pronostics/grille`
- `/pronostics/leaderboard`
- `/live`
- `/quiz/supporter`
- `/calendrier/imprimer`
- `/calendrier/jour-[n]`

**Améliorations systémiques pour ces pages** :
1. Ajouter hero `hero-animated` (même pour pages légales, pour cohérence)
2. Ajouter breadcrumb (composant)
3. Ajouter internal links (footer CTA vers pages principales)
4. Améliorer design avec glassmorphism cards au lieu de bg-white plat
5. Ajouter ANJBanner sur toutes les pages liées aux paris

---

### 🔴 Pages D (Faible, refonte nécessaire)

#### **Admin** `/admin` — Note : D
- Page admin, pas dans le scope de l'audit UX public

---

## 🚀 Top 10 des améliorations à plus fort impact (Quick Wins)

### 1. **Standardiser tous les hero avec `hero-animated`**
**Impact** : ⭐⭐⭐⭐⭐  
**Effort** : Faible  
**Pages concernées** : Groupes, Stades, Quiz, + toutes les pages C

**Avant** :
```tsx
<HeroSection title="..." subtitle="..." className="bg-primary" />
```

**Après** :
```tsx
<section className="hero-animated text-white py-12 sm:py-16">
  <div className="relative z-10 mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
    <h1 className="text-2xl font-extrabold sm:text-4xl">Titre</h1>
    <p className="mt-2 text-gray-300">Subtitle</p>
  </div>
</section>
```

**Pages prioritaires** :
- `/groupes` ✅
- `/stades` ✅
- `/quiz` ✅
- Toutes les pages légales (mentions, politique, contact, etc.)

---

### 2. **Ajouter FAQ JSON-LD sur toutes les pages principales**
**Impact** : ⭐⭐⭐⭐⭐ (SEO boost énorme)  
**Effort** : Moyen  
**Pages concernées** : Groupes, Groupe [lettre], Buteurs, Match [slug], Simulateur, Histoire, Actualités, Paris sportifs, Joueurs, Tableau, Quiz

**Template** :
```tsx
const faqItems = [
  { question: "...", answer: "..." },
];

const faqJsonLd = {
  "@context": "https://schema.org",
  "@type": "FAQPage",
  mainEntity: faqItems.map((item) => ({
    "@type": "Question",
    name: item.question,
    acceptedAnswer: { "@type": "Answer", text: item.answer },
  })),
};

// Dans le JSX :
<FAQSection title="❓ Questions fréquentes — [Titre]" items={faqItems} />
<script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(faqJsonLd) }} />
```

**Exemples de questions par page** :
- **/groupes** : "Combien d'équipes se qualifient par groupe ?", "Comment sont déterminés les meilleurs 3e ?"
- **/groupe/[lettre]** : "Qui va se qualifier dans le groupe X ?", "Quelles sont les forces de l'équipe Y ?"
- **/buteurs** : "Qui est le meilleur buteur de l'histoire ?", "Qui va gagner le Soulier d'Or 2026 ?"
- **/simulateur** : "Comment fonctionne le simulateur ?", "Puis-je partager mon bracket ?"

---

### 3. **Uniformiser les breadcrumbs avec le composant `<Breadcrumb>`**
**Impact** : ⭐⭐⭐  
**Effort** : Très faible  
**Pages concernées** : Toutes les pages qui utilisent HTML inline pour breadcrumbs

**Avant** (HTML inline) :
```tsx
<nav className="bg-white dark:bg-slate-900 border-b border-gray-200 dark:border-gray-700">
  <div className="mx-auto max-w-7xl px-4 py-3">
    <ol className="flex items-center gap-2 text-sm text-gray-500">
      <li><Link href="/">Accueil</Link></li>
      <li>/</li>
      <li>Page</li>
    </ol>
  </div>
</nav>
```

**Après** (composant) :
```tsx
import { Breadcrumb } from "@repo/ui/breadcrumb";

<Breadcrumb
  items={[
    { label: "Accueil", href: "/" },
    { label: "Page" },
  ]}
/>
```

**Bénéfices** :
- Cohérence visuelle
- Moins de code dupliqué
- Maintenance facilitée

---

### 4. **Ajouter bouton "Partager" Web Share API sur pages clés**
**Impact** : ⭐⭐⭐⭐ (engagement)  
**Effort** : Faible  
**Pages concernées** : Match [slug], Simulateur, Quiz (après completion), Pronostic [slug]

**Composant** (à créer) :
```tsx
// apps/fr/app/components/ShareButton.tsx
"use client";

interface ShareButtonProps {
  title: string;
  text: string;
  url: string;
}

export function ShareButton({ title, text, url }: ShareButtonProps) {
  const handleShare = async () => {
    if (navigator.share) {
      try {
        await navigator.share({ title, text, url });
      } catch (err) {
        console.log("Share cancelled");
      }
    } else {
      // Fallback: copy to clipboard
      navigator.clipboard.writeText(url);
      alert("Lien copié !");
    }
  };

  return (
    <button
      onClick={handleShare}
      className="inline-flex items-center gap-2 rounded-lg border border-white/20 bg-white/10 px-4 py-2.5 text-sm font-medium text-white hover:bg-white/20 transition-colors"
    >
      🔗 Partager
    </button>
  );
}
```

**Usage** :
```tsx
<ShareButton 
  title={`${home.name} vs ${away.name} - CDM 2026`}
  text={`Découvre le match ${home.name} vs ${away.name} !`}
  url={`https://cdm2026.fr/match/${match.slug}`}
/>
```

---

### 5. **Enrichir `/groupe/[lettre]` avec sections storytelling**
**Impact** : ⭐⭐⭐⭐  
**Effort** : Moyen  
**Pages concernées** : Groupe [lettre] (12 pages)

**Sections à ajouter** :
1. **"🎭 L'histoire du groupe"** : anecdotes, rivalités historiques
2. **"⭐ Joueurs à suivre"** : 1-2 joueurs clés par équipe (avec mini-cards)
3. **"📊 Statistiques H2H"** : historique confrontations entre les équipes du groupe
4. **"🔮 Notre pronostic"** : qui va se qualifier et pourquoi (storytelling)

**Template** :
```tsx
<section className="rounded-xl bg-white dark:bg-slate-800 p-6 shadow-sm">
  <h2 className="text-2xl font-bold mb-4">⭐ Joueurs à suivre</h2>
  <div className="grid sm:grid-cols-2 gap-4">
    {players.map((player) => (
      <div key={player.id} className="rounded-lg border border-gray-200 p-4 hover:border-primary/30 transition-colors">
        <div className="flex items-center gap-3">
          <span className="text-2xl">{team.flag}</span>
          <div>
            <p className="font-bold">{player.name}</p>
            <p className="text-sm text-gray-500">{player.position} · {player.club}</p>
          </div>
        </div>
        <p className="text-xs text-gray-600 mt-2">{player.insight}</p>
      </div>
    ))}
  </div>
</section>
```

---

### 6. **Intégrer BetOfTheDay sur pages Match [slug] et Homepage**
**Impact** : ⭐⭐⭐⭐ (conversion paris)  
**Effort** : Très faible  
**Pages concernées** : Homepage (déjà présent ?), Match [slug]

**Sur Homepage** (après UpcomingMatches) :
```tsx
import { BetOfTheDay } from "./components/BetOfTheDay";

// Dans HomePage() :
<section className="bg-white dark:bg-slate-900 py-16">
  <div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
    <BetOfTheDay />
  </div>
</section>
```

**Sur Match [slug]** (dans la sidebar) :
```tsx
<BetOfTheDay compact bet={matchBet} />
```

**Bénéfices** :
- Augmente l'engagement paris sportifs
- CTA clairs vers bookmakers
- Design premium cohérent

---

### 7. **Créer composant `<PremiumCard>` réutilisable**
**Impact** : ⭐⭐⭐⭐⭐ (cohérence design)  
**Effort** : Moyen  
**Pages concernées** : Toutes

**Composant** (à créer) :
```tsx
// packages/ui/src/premium-card.tsx
import { type ReactNode } from "react";

interface PremiumCardProps {
  children: ReactNode;
  className?: string;
  hover?: boolean;
}

export function PremiumCard({ children, className = "", hover = true }: PremiumCardProps) {
  return (
    <div
      className={`
        rounded-xl border border-gray-200 dark:border-slate-700 
        bg-white dark:bg-slate-800 
        shadow-sm 
        ${hover ? "hover:shadow-md hover:border-primary/30 transition-all" : ""}
        ${className}
      `}
    >
      {children}
    </div>
  );
}
```

**Usage** :
```tsx
<PremiumCard>
  <div className="p-6">
    {/* Contenu */}
  </div>
</PremiumCard>
```

**Remplace** tous les :
```tsx
<div className="rounded-lg bg-white dark:bg-slate-800 p-6 shadow-sm">
```

**Bénéfices** :
- Cohérence visuelle 100%
- Design system unifié
- Maintenance facilitée (un seul endroit pour changer le style)

---

### 8. **Ajouter ANJBanner sur TOUTES les pages liées aux paris**
**Impact** : ⭐⭐⭐⭐⭐ (compliance légale)  
**Effort** : Très faible  
**Pages concernées** : Paris sportifs, Bookmaker [slug], Comparateur cotes, Pronostic vainqueur, Pronostic [slug], Pronostic match [slug], Buteurs

**Code** :
```tsx
import { ANJBanner } from "@repo/ui/anj-banner";

// En bas de page (avant </>) :
<ANJBanner />
```

**Bénéfices** :
- Conformité ANJ/ARJEL
- Protection juridique
- Trust utilisateur

---

### 9. **Améliorer contraste texte sur cards glassmorphism**
**Impact** : ⭐⭐⭐ (accessibilité)  
**Effort** : Faible  
**Pages concernées** : Toutes les pages avec cards glassmorphism

**Problème actuel** : Certaines cards utilisent `bg-white/5` avec texte clair sur fond clair → contraste insuffisant

**Solution** :
```tsx
// ❌ AVANT (contraste faible)
<div className="rounded-xl bg-white/5 border-white/10 p-4">
  <p className="text-white/70">Texte</p>
</div>

// ✅ APRÈS (contraste OK)
<div className="rounded-xl bg-white/10 dark:bg-white/5 border-white/20 dark:border-white/10 p-4 backdrop-blur-sm">
  <p className="text-white dark:text-gray-200">Texte</p>
</div>
```

**Vérifier** :
- Toutes les cards avec `bg-white/5` ou `bg-white/10`
- Contraste minimum WCAG AA : 4.5:1

---

### 10. **Ajouter internal linking systématique en bas de page**
**Impact** : ⭐⭐⭐⭐ (SEO + UX)  
**Effort** : Faible  
**Pages concernées** : Toutes

**Composant** `<RelatedContent>` (existe déjà, à généraliser) :
```tsx
// packages/ui/src/related-content.tsx
interface RelatedItem {
  href: string;
  emoji: string;
  title: string;
  description: string;
}

interface RelatedContentProps {
  items: RelatedItem[];
}

export function RelatedContent({ items }: RelatedContentProps) {
  return (
    <section className="bg-gray-50 dark:bg-slate-900/50 py-12">
      <div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
        <h2 className="text-xl font-bold mb-6">À découvrir aussi</h2>
        <div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">
          {items.map((item) => (
            <Link
              key={item.href}
              href={item.href}
              className="group rounded-xl border border-gray-200 dark:border-slate-700 bg-white dark:bg-slate-800 p-4 hover:border-primary/30 hover:shadow-md transition-all"
            >
              <span className="text-2xl mb-2 block">{item.emoji}</span>
              <h3 className="font-bold text-sm group-hover:text-primary transition-colors">{item.title}</h3>
              <p className="text-xs text-gray-500 mt-1">{item.description}</p>
            </Link>
          ))}
        </div>
      </div>
    </section>
  );
}
```

**Usage** (exemple pour `/groupes`) :
```tsx
<RelatedContent
  items={[
    { href: '/equipes', emoji: '🌍', title: '48 équipes', description: 'Toutes les sélections qualifiées' },
    { href: '/pronostic-vainqueur', emoji: '🏆', title: 'Qui va gagner ?', description: 'Notre pronostic vainqueur' },
    { href: '/simulateur', emoji: '🎮', title: 'Simulateur', description: 'Créez votre bracket' },
    { href: '/match/calendrier', emoji: '📅', title: 'Calendrier', description: '104 matchs à suivre' },
  ]}
/>
```

**Bénéfices** :
- SEO : crawlabilité améliorée
- UX : découverte de contenu
- Engagement : temps sur site ↑

---

## 🏗️ Améliorations systémiques (Patterns à appliquer partout)

### 1. **Design System — Composants réutilisables**

Créer dans `packages/ui/src/` :

#### `<PremiumHeroAnimated>`
```tsx
interface PremiumHeroAnimatedProps {
  title: string;
  subtitle: string;
  badge?: { icon: string; text: string };
  ctas?: Array<{ href: string; label: string; variant: 'primary' | 'secondary' | 'tertiary' }>;
}

export function PremiumHeroAnimated({ title, subtitle, badge, ctas }: PremiumHeroAnimatedProps) {
  return (
    <section className="hero-animated text-white py-12 sm:py-16">
      <div className="relative z-10 mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
        {badge && (
          <div className="inline-flex items-center gap-2 rounded-full bg-secondary/15 border border-secondary/30 px-3 py-1 text-xs font-bold text-secondary mb-3">
            <span>{badge.icon}</span>
            {badge.text}
          </div>
        )}
        <h1 className="text-2xl font-extrabold sm:text-4xl">{title}</h1>
        <p className="mt-2 text-gray-300">{subtitle}</p>
        {ctas && (
          <div className="flex flex-wrap gap-3 mt-6">
            {ctas.map((cta) => (
              <Link
                key={cta.href}
                href={cta.href}
                className={
                  cta.variant === 'primary'
                    ? 'rounded-lg bg-accent px-5 py-2.5 font-bold text-white hover:bg-accent/80 transition-all'
                    : cta.variant === 'secondary'
                    ? 'rounded-lg border border-white/20 bg-white/10 px-5 py-2.5 font-semibold text-white hover:bg-white/20 transition-all'
                    : 'rounded-lg border border-secondary/40 bg-secondary/15 px-5 py-2.5 font-semibold text-secondary hover:bg-secondary/25 transition-all'
                }
              >
                {cta.label}
              </Link>
            ))}
          </div>
        )}
      </div>
    </section>
  );
}
```

---

#### `<PremiumCard>` (déjà décrit plus haut)

---

#### `<PremiumStat>` (pour les stats pills)
```tsx
interface PremiumStatProps {
  value: string;
  label: string;
  icon?: string;
}

export function PremiumStat({ value, label, icon }: PremiumStatProps) {
  return (
    <div className="rounded-xl bg-white/10 dark:bg-white/5 border border-white/20 dark:border-white/10 px-6 py-3 text-center backdrop-blur-sm">
      {icon && <span className="text-xl mb-1 block">{icon}</span>}
      <p className="text-2xl font-extrabold text-white">{value}</p>
      <p className="text-xs text-gray-300 mt-1">{label}</p>
    </div>
  );
}
```

---

### 2. **Pattern de page type "Liste"** (Équipes, Groupes, Stades, etc.)

Template :
```tsx
export default function ListPage() {
  return (
    <>
      <Breadcrumb items={[...]} />
      
      <PremiumHeroAnimated
        title="..."
        subtitle="..."
        badge={{ icon: "🌍", text: "48 équipes" }}
      />

      {/* Stats ribbon */}
      <section className="bg-white dark:bg-primary border-y border-gray-200 dark:border-white/5">
        <div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
          <div className="grid grid-cols-4 divide-x divide-gray-200 dark:divide-white/5">
            {stats.map((stat) => (
              <PremiumStat key={stat.label} {...stat} />
            ))}
          </div>
        </div>
      </section>

      {/* Main content */}
      <div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-10 sm:py-12">
        <PremiumCard>
          {/* Table ou Grid */}
        </PremiumCard>
      </div>

      <FAQSection title="..." items={faqItems} />
      <RelatedContent items={relatedItems} />
      <Newsletter variant="banner" />
      <ANJBanner /> {/* Si paris */}

      {/* JSON-LD */}
      <script type="application/ld+json" {...} />
    </>
  );
}
```

---

### 3. **Pattern de page type "Détail"** (Équipe [slug], Match [slug], etc.)

Template :
```tsx
export default function DetailPage() {
  return (
    <>
      <BreadcrumbSchema items={[...]} baseUrl={domains.fr} />
      <Breadcrumb items={[...]} />
      
      <PremiumHero {...} /> {/* Hero custom par type */}

      <div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-10 sm:py-12">
        <div className="grid gap-8 lg:grid-cols-3">
          {/* Main content (2 cols) */}
          <div className="lg:col-span-2 space-y-8">
            <PremiumCard>{/* Section 1 */}</PremiumCard>
            <PremiumCard>{/* Section 2 */}</PremiumCard>
            {/* ... */}
          </div>

          {/* Sidebar (1 col) */}
          <div className="space-y-6">
            <PremiumCard>{/* Info box */}</PremiumCard>
            <BetOfTheDay compact /> {/* Si paris */}
          </div>
        </div>
      </div>

      <FAQSection title="..." items={faqItems} />
      <Newsletter variant="banner" />
      <FinalCTA />
      <ANJBanner /> {/* Si paris */}

      {/* JSON-LD */}
      <script type="application/ld+json" {...} />
    </>
  );
}
```

---

### 4. **Checklist SEO complète pour chaque page**

- [ ] Metadata complète (title, description, OG)
- [ ] Alternates (i18n) si applicable
- [ ] Canonical URL
- [ ] Breadcrumb (component `<Breadcrumb>`)
- [ ] BreadcrumbSchema (JSON-LD)
- [ ] FAQ Section (composant `<FAQSection>`)
- [ ] FAQ JSON-LD (schema.org/FAQPage)
- [ ] Entity-specific JSON-LD (SportsTeam, SportsEvent, etc.)
- [ ] Internal linking (RelatedContent)
- [ ] Newsletter signup
- [ ] ANJBanner (si paris)
- [ ] Share button (si pertinent)

---

### 5. **Checklist Design complète pour chaque page**

- [ ] Hero `hero-animated` ou custom premium (gradient + glassmorphism)
- [ ] Badge pill pour catégories/tags (rounded-full, uppercase, tracking-widest)
- [ ] Grands drapeaux emoji pour équipes (text-4xl ou plus)
- [ ] CTA primaire = `bg-accent`
- [ ] CTA secondaire = `border-white/20 bg-white/10`
- [ ] Cards = glassmorphism (`PremiumCard` component)
- [ ] Contraste texte OK (WCAG AA minimum)
- [ ] Responsive (mobile-first)
- [ ] Hover states (hover:shadow-md, hover:border-primary/30, etc.)
- [ ] Dark mode support (dark: variants)

---

### 6. **Palette de couleurs — Respect strict**

```css
/* ✅ À UTILISER */
--color-primary: #0D3B66;     /* Bleu nuit */
--color-accent: #00B865;      /* Vert émeraude (CTA) */
--color-secondary: #FFB800;   /* Or (accents) */
--color-field: #2A9D8F;       /* Vert field (backgrounds) */
--color-deep: #001F3F;        /* Bleu profond (backgrounds) */

/* ❌ JAMAIS UTILISER */
--color-orange: #FF6B35;      /* Orange — BANNI */
--color-turquoise: #2EC4B6;  /* Turquoise — BANNI */
```

**Dégradés autorisés** :
```css
/* ✅ Bleu nuit → bleu profond */
background: linear-gradient(135deg, #0D3B66 0%, #001F3F 100%);

/* ✅ Vert émeraude → vert field */
background: linear-gradient(135deg, #00B865 0%, #2A9D8F 100%);

/* ❌ JAMAIS bleu → or (looks cheap) */
background: linear-gradient(135deg, #0D3B66 0%, #FFB800 100%); /* ❌ */
```

---

## 📈 Métriques de succès

**Avant l'audit** :
- 73 pages analysées
- ~40% avec hero premium
- ~30% avec FAQ JSON-LD
- ~50% avec breadcrumb component
- Incohérence design (certaines pages basiques)

**Objectif post-améliorations** :
- 100% pages avec hero `hero-animated` ou custom premium
- 80% pages avec FAQ JSON-LD (hors pages légales)
- 100% pages avec breadcrumb component
- 100% pages avec `PremiumCard` component
- 100% pages avec internal linking (RelatedContent)
- 100% pages liées aux paris avec ANJBanner
- Design system unifié (même look & feel partout)

---

## 🎯 Priorisation des pages à améliorer

### 🔴 **Priorité HAUTE** (trafic attendu élevé)
1. Homepage → déjà A, RAS
2. Équipe de France → déjà A+, RAS
3. Équipes → note A, ajouter FAQ
4. Groupes → note B+, **hero-animated + FAQ**
5. Groupe [lettre] → note B+, **FAQ + sections storytelling**
6. Match [slug] → note A, ajouter FAQ + Share button
7. Calendrier → note A, RAS
8. Pronostic vainqueur → note A, RAS
9. Simulateur → note A, ajouter FAQ + Share button
10. Buteurs → note A-, **ajouter FAQ**

### 🟠 **Priorité MOYENNE**
11. Billets → note A, RAS
12. Quiz → note B, **hero-animated + FAQ + Share button**
13. Stades → note A, **hero-animated + FAQ**
14. Histoire → note A, ajouter FAQ
15. Actualités → note B+, **FAQ + filtres catégorie**
16. Paris sportifs → note B+, **FAQ + BetOfTheDay**
17. Joueurs → note B+, **FAQ + section "Joueurs à suivre 2026"**
18. Tableau → note B+, **FAQ JSON-LD**
19. FAQ → note A, RAS

### 🟢 **Priorité BASSE**
20-73. Toutes les autres pages (C et D)
- Ajouter hero `hero-animated`
- Ajouter breadcrumb component
- Ajouter RelatedContent
- Uniformiser avec `PremiumCard`

---

## 🛠️ Plan d'action recommandé

### **Phase 1 : Quick Wins (1-2 jours)**
1. Standardiser hero `hero-animated` sur Groupes, Stades, Quiz
2. Ajouter FAQ JSON-LD sur Buteurs, Groupes, Groupe [lettre], Simulateur
3. Uniformiser breadcrumbs avec composant `<Breadcrumb>`
4. Ajouter ANJBanner sur toutes les pages paris

### **Phase 2 : Enrichissement contenu (3-5 jours)**
5. Enrichir `/groupe/[lettre]` avec sections storytelling
6. Ajouter FAQ sur Histoire, Actualités, Paris sportifs, Joueurs, Tableau
7. Intégrer BetOfTheDay sur Homepage et Match [slug]
8. Ajouter Share button sur Match, Simulateur, Quiz

### **Phase 3 : Design System (2-3 jours)**
9. Créer composants `<PremiumCard>`, `<PremiumHeroAnimated>`, `<PremiumStat>`
10. Remplacer toutes les cards basiques par `<PremiumCard>`
11. Ajouter RelatedContent en bas de toutes les pages
12. Vérifier contraste texte partout (accessibilité)

### **Phase 4 : Pages légales & long tail (1-2 jours)**
13. Améliorer design pages légales (mentions, politique, contact, etc.)
14. Ajouter hero + breadcrumb + internal linking
15. QA visuelle complète (scripts/visual-qa.sh)

---

## ✅ Conclusion

Le site **cdm2026.fr** a déjà un **excellent niveau de qualité** sur les pages principales (homepage, équipe de France, équipes, match [slug], pronostic vainqueur, simulateur, billets). 

**Points forts actuels** :
- Hero premium `hero-animated` sur la majorité des pages clés
- Glassmorphism cohérent (bg-white/10, border-white/20, backdrop-blur)
- CTA clairs (bg-accent primaire, border-white/20 bg-white/10 secondaire)
- SEO solide (metadata, JSON-LD, breadcrumbs schema)
- Composants réutilisables bien conçus (PremiumHero, FAQSection, etc.)

**Axes d'amélioration prioritaires** :
1. **Uniformiser hero-animated** sur 100% des pages (currently ~40%)
2. **Ajouter FAQ JSON-LD** sur toutes les pages principales (SEO boost)
3. **Enrichir contenu** des pages "liste" (Groupes, Groupe [lettre]) avec storytelling
4. **Créer design system** complet (PremiumCard, PremiumHeroAnimated, PremiumStat)
5. **Améliorer pages légales** (actuellement très basiques)

**Estimation effort total** : ~10-12 jours de développement.

**Impact attendu** :
- SEO : +30% trafic organique (FAQ JSON-LD + internal linking)
- UX : +20% temps sur site (contenu enrichi + related content)
- Conversion : +15% clics bookmakers (BetOfTheDay + ANJBanner partout)
- Design : 100% cohérence visuelle (design system)

---

**Prêt pour la validation Xavier ! 🚀**
